import 'package:flutter/material.dart';

class PhotoCamera extends StatelessWidget {
  const PhotoCamera({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Camera'),
    );
  }
}